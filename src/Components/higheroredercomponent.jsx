// * A higher order component is a function that takes in a component and return a new component.
// * higher order component is the advance method of reusing the component functionality logic.
// *it simply takes the original component and returns this enhance component.
// syntax:-const Enhancecomponent=higherordercomponent(original Component)

// * new component:-it will be the enhance component.
// * higherfunction:-as the suggest this function will enhance will enhance wrapped component.
// * wrapped component:-the component whose functionality we want to extend in other words,this will be the component that we want to enhance.
// * enhance component:-higher order component are a design pattern in react where a function takes a component as an argument and returns an ehanced new component.Component